# OpenReel Studio · API

FastAPI backend. See repo root README for the full architecture.

## 中文摘要

这里是 OpenReel Studio 的 FastAPI 后端服务。完整项目结构、启动方式和
Agent/工作流架构请先看仓库根目录的 `README.md` 和 `SETUP.md`。

```bash
uv sync
uv run uvicorn app.main:app --reload --port 8000
```
