services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    env_file:
      - .env.production
    environment:
      DRAMA_PROMPT_DUMP_ENABLED: "true"
      DRAMA_PROMPT_DUMP_FULL: "false"
      DRAMA_PROMPT_DUMP_INCLUDE_CONTENT: "false"
      DRAMA_PROMPT_DUMP_RETENTION_DAYS: "3"
      DRAMA_PROMPT_DUMP_DIR: /workspace/data/prompt_dumps
      DRAMA_NODE_RUN_TIMEOUT_SECONDS: "600"
      DRAMA_VIDEO_POLL_TIMEOUT_SECONDS: "1200"
      OPENREEL_SKILLS_DIR: /workspace/skills
    volumes:
      - ./data:/workspace/data
      - ./storage:/workspace/storage
      - ./assets:/workspace/assets
      - ./config:/workspace/config
      - ./plugins:/workspace/plugins
      - ./skills:/workspace/skills
      - ./workflow_templates:/workspace/workflow_templates
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_BASE_PATH: /studio
        NEXT_PUBLIC_API_BASE_URL: /studio
    environment:
      INTERNAL_API_BASE_URL: http://api:8000
    depends_on:
      - api
    restart: unless-stopped

  gateway:
    image: caddy:2-alpine
    env_file:
      - .env.production
    ports:
      - "${APP_PUBLIC_PORT:-3100}:3000"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./deploy/certs:/etc/caddy/certs:ro
    depends_on:
      - web
    restart: unless-stopped
